## Project Setup To Run App Locally

#Create a .env file with the following values:
JWT_LIFETIME
JWT_SECRET
MONGO_URL
PORT

#Run the following commands in the terminal:

```sh
npm install
npm start
```
